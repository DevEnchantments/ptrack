/**
 * Shared help copy for the dialog forms. Lives in lib/ because component files
 * may export only components (react-refresh/only-export-components).
 */
export const GOLD_HELP =
  'Marking a record as "Gold" ensures that it appears at the top of its ' +
  'section on the project details page and is always included in project ' +
  'details emails. If a record is not marked as "Gold" it might be omitted ' +
  'from project details emails (depending on the number of records in the ' +
  'project and the date that it was created).'
